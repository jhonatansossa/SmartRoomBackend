GET information item status given an id
---
tags:
  - Devices

parameters:
  - in: header
    name: Authorization
    required: true
  - in: path
    name: itemid
    required: true

responses:
  200:
    description: Item status is retrieved

  404:
    description: Item does not exist